{
  "scripts": {
    "build:css": "tailwindcss -i app/src/styles/style.css -o app/dist/style.css",
    "build:ts": "ts-node scripts/esbuild.ts",
    "build": "npm run build:ts && npm run build:css"
  },
  "dependencies": {
    "esbuild-plugin-tailwindcss": "^1.2.0",
    "esbuild-svelte": "^0.8.0",
    "fuzzy-search": "^3.2.1",
    "svelte": "^4.2.15",
    "svelte-preprocess-esbuild": "^3.0.1"
  },
  "devDependencies": {
    "@types/fuzzy-search": "^2.1.5",
    "concurrently": "^8.2.2",
    "esbuild": "0.20.2",
    "tailwindcss": "^3.4.3"
  }
}
